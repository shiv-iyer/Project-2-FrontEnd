/* Run the Express app first, hosting the server on port 3000 */

/* OLD API: http://localhost:3000/ */

// const BASE_API = "https://dwad22-project2-backend.onrender.com/"
const BASE_API = "http://localhost:3000/";

/* export to the other files, then pass in to whatever components you need */

export default BASE_API;